// EXAMPLE.wfs : main

package:
    - name: main

    /* 
    Variable definitions
    */

    define:
        - greeting: hello
        - name: jeff

    /* The code that is run */
    
    run:

        - log: ${{ greeting }} world from ${{ js }}

    loop:
        - loops: 10
        - log: hello
